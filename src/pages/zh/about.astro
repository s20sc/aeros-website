---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="关于我们 — AEROS" description="AEROS 的使命：为具身智能体打造可治理的运行时底座。" lang="zh">

  <!-- ═══ Mission ═══ -->
  <section class="section" style="padding-bottom: var(--space-2xl);">
    <div class="container">
      <div class="section-label">关于我们</div>
      <h1 class="page-title">
        给具身智能体<br>一个配得上的底座
      </h1>
      <p class="page-desc">
        机器人的未来，不只是更强的模型、更好的硬件。
        关键在于：谁来定义智能体真正<em>运行</em>的那一层。
      </p>
    </div>
  </section>

  <!-- ═══ Mission Statement ═══ -->
  <section class="section section-alt">
    <div class="container">
      <div class="mission-block">
        <div class="mission-quote">
          <p>
            我们做的事很简单：给具身智能体一个跑得稳、管得住、能进化的运行时。
          </p>
          <p class="mission-sub">
            不是又一个机器人 OS，也不是模型的包装层。
            我们要填的是"Demo 很酷"到"量产能用"之间那道鸿沟——让它变得可管理、可审计、真正安全。
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ Why This Matters ═══ -->
  <section class="section">
    <div class="container">
      <div class="section-label">核心论点</div>
      <h2 class="section-title">为什么智能体需要运行时</h2>

      <div class="thesis-grid">
        <div class="thesis-card">
          <div class="thesis-num">01</div>
          <h3>光有模型远远不够</h3>
          <p>大模型给了智能体推理能力。但没有治理、没有状态管理、没有生命周期管控，那只是一个漂亮的 Demo。</p>
        </div>
        <div class="thesis-card">
          <div class="thesis-num">02</div>
          <h3>硬件越来越碎片化</h3>
          <p>人形、四足、AMR、无人机、协作臂——每种形态能力都不一样。能力必须模块化、可迁移、可版本管理。</p>
        </div>
        <div class="thesis-card">
          <div class="thesis-num">03</div>
          <h3>上了生产就得有治理</h3>
          <p>在仓库、医院、公共场所，"刚才发生了什么？""谁批准的？"这些问题必须有答案。运行时治理就是答案。</p>
        </div>
      </div>
    </div>
  </section>


  <!-- ═══ Milestones ═══ -->
  <section class="section">
    <div class="container">
      <div class="section-label">时间线</div>
      <h2 class="section-title">里程碑</h2>

      <div class="timeline">
        <div class="timeline-item done">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <span class="timeline-date">2026.04</span>
            <h3>7 篇论文完成</h3>
            <p>系统性研究收官：运行时、治理、进化、契约、联邦化、基准测试。</p>
          </div>
        </div>
        <div class="timeline-item done">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <span class="timeline-date">2026.04</span>
            <h3>arXiv 预印本上线</h3>
            <p>4 篇论文公开发布，接受同行评审。</p>
          </div>
        </div>
        <div class="timeline-item done">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <span class="timeline-date">2026.04</span>
            <h3>开源 MVP 发布</h3>
            <p>运行时核心 Apache 2.0 开源，项目主页同步上线。</p>
          </div>
        </div>
        <div class="timeline-item active">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <span class="timeline-date">2026.H2</span>
            <h3>运行时核心 v1.0</h3>
            <p>生产可用的运行时，完整生命周期管理、ECM 支持、全链路追踪。</p>
          </div>
        </div>
        <div class="timeline-item">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <span class="timeline-date">2026.H2</span>
            <h3>首个试点落地</h3>
            <p>和仓储自动化合作伙伴一起做 POC 验证。</p>
          </div>
        </div>
        <div class="timeline-item">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <span class="timeline-date">2027</span>
            <h3>治理控制台发布</h3>
            <p>企业级治理产品，车队级策略执行和审计。</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ Investor CTA ═══ -->
  <section class="section section-alt" style="text-align: center;">
    <div class="container">
      <h2 class="section-title">致投资人</h2>
      <p class="section-desc" style="margin: 0 auto var(--space-xl);">
        AEROS 在定义一个新品类——具身 AI 的运行时层。
        我们在找真正懂基础设施投资的伙伴。
      </p>
      <div style="display: flex; gap: var(--space-md); justify-content: center; flex-wrap: wrap;">
        <a href="/zh/contact" class="btn btn-primary">预约沟通</a>
        <a href="/zh/contact" class="btn btn-secondary">索取 BP</a>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  .page-title {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 700;
    line-height: 1.05;
    letter-spacing: -0.02em;
    color: var(--text-primary);
    margin-bottom: var(--space-md);
  }
  .page-title em { font-style: normal; color: var(--primary); }
  .page-desc {
    font-size: 1.125rem;
    color: var(--text-secondary);
    max-width: 560px;
    line-height: 1.7;
  }
  .page-desc em { font-style: normal; color: var(--primary); font-weight: 600; }
  .section-alt {
    background: var(--bg-surface);
    border-top: 1px solid var(--border-subtle);
    border-bottom: 1px solid var(--border-subtle);
  }

  /* ═══ Mission ═══ */
  .mission-block {
    max-width: 720px;
  }
  .mission-quote p {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 600;
    color: var(--text-primary);
    line-height: 1.3;
    margin-bottom: var(--space-lg);
  }
  .mission-sub {
    font-family: var(--font-body) !important;
    font-size: 1rem !important;
    font-weight: 400 !important;
    color: var(--text-secondary) !important;
    line-height: 1.7 !important;
  }

  /* ═══ Thesis ═══ */
  .thesis-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
  }
  .thesis-card {
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
  }
  .thesis-num {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--primary);
    margin-bottom: var(--space-md);
  }
  .thesis-card h3 {
    font-family: var(--font-display);
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--space-sm);
    line-height: 1.3;
  }
  .thesis-card p {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* ═══ Team ═══ */
  .team-grid {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    margin-bottom: var(--space-xl);
  }
  .team-card {
    display: flex;
    gap: var(--space-xl);
    background: var(--bg-base);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    align-items: start;
  }
  .team-avatar {
    width: 72px;
    height: 72px;
    border-radius: var(--radius-lg);
    background: rgba(0, 102, 255, 0.1);
    color: var(--primary);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 700;
    flex-shrink: 0;
  }
  .team-info h3 {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 2px;
  }
  .team-role {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--primary);
    margin-bottom: var(--space-md);
  }
  .team-bio {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.6;
  }
  .team-hiring {
    font-size: 14px;
    color: var(--text-muted);
    padding: var(--space-md) var(--space-lg);
    background: var(--bg-base);
    border: 1px dashed var(--border);
    border-radius: var(--radius-md);
  }
  .team-hiring a {
    color: var(--primary);
    font-weight: 500;
  }

  /* ═══ Timeline ═══ */
  .timeline {
    max-width: 640px;
    position: relative;
    padding-left: var(--space-xl);
  }
  .timeline::before {
    content: '';
    position: absolute;
    left: 7px;
    top: 8px;
    bottom: 8px;
    width: 2px;
    background: var(--border);
  }
  .timeline-item {
    position: relative;
    padding-bottom: var(--space-xl);
  }
  .timeline-item:last-child { padding-bottom: 0; }

  .timeline-marker {
    position: absolute;
    left: calc(-1 * var(--space-xl) + 2px);
    top: 6px;
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--bg-base);
    border: 2px solid var(--border);
  }
  .done .timeline-marker {
    background: var(--success);
    border-color: var(--success);
  }
  .active .timeline-marker {
    background: var(--primary);
    border-color: var(--primary);
    box-shadow: 0 0 0 4px rgba(0, 102, 255, 0.2);
  }

  .timeline-date {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--text-muted);
  }
  .timeline-content h3 {
    font-family: var(--font-display);
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--text-primary);
    margin: var(--space-xs) 0;
  }
  .timeline-content p {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.5;
  }
  .timeline-item:not(.done):not(.active) h3 { color: var(--text-muted); }
  .timeline-item:not(.done):not(.active) p { color: var(--text-muted); }

  @media (max-width: 768px) {
    .thesis-grid { grid-template-columns: 1fr; }
    .team-card { flex-direction: column; }
  }
</style>
