---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="AEROS — 让具身智能体跑起来" lang="zh">

  <!-- ═══ Section 1: Hero ═══ -->
  <section class="hero grid-bg">
    <div class="container hero-content">
      <h1 class="hero-h1">
        <em>具身智能体</em>的<br>运行时
      </h1>
      <p class="hero-sub">
        一个机器人，一个 Agent，一个 Runtime。<br>
        让具身智能体真正跑起来——稳定运行、受控治理、持续进化。
      </p>
      <div class="hero-ctas">
        <a href="/zh/contact" class="btn btn-primary">预约 Demo</a>
        <a href="https://github.com/s20sc/aeros-runtime" target="_blank" rel="noopener" class="btn btn-secondary">
          GitHub 源码 <span class="arrow">&rarr;</span>
        </a>
      </div>
      <p class="hero-badge">运行时核心开源 &middot; Apache 2.0</p>
    </div>
  </section>

  <!-- ═══ Section 2: Problem Statement ═══ -->
  <section class="section">
    <div class="container">
      <div class="section-label">行业缺口</div>
      <h2 class="section-title">大模型会思考了，机器人能动了，<br>但智能体还是跑不起来。</h2>
      <p class="section-desc" style="margin-bottom: var(--space-2xl)">
        差的就是那一层运行时——让智能体在真实世界里稳稳跑起来的系统底座。
      </p>

      <div class="problem-grid">
        <div class="problem-card">
          <div class="problem-icon">&#x1D6CC;</div>
          <h3>模型够强了，但不够用</h3>
          <p>大模型已经能理解动作、物体和环境。但能推理不等于能执行，智能体光靠模型跑不起来。</p>
        </div>
        <div class="problem-card">
          <div class="problem-icon">&#x25C7;</div>
          <h3>硬件百花齐放，软件各自为政</h3>
          <p>人形、四足、AMR、协作臂纷纷量产。形态越来越多，但缺一个通用的运行时把它们管起来。</p>
        </div>
        <div class="problem-card">
          <div class="problem-icon">&#x25B3;</div>
          <h3>出了实验室，规则全变了</h3>
          <p>智能体一旦到了真实场景，稳定性、故障恢复、安全合规、行为审计全都变成刚需。</p>
        </div>
      </div>

      <div class="problem-conclusion">
        <p>行业真正缺的，是让智能体<strong>跑得稳、管得住、规模化得了</strong>的运行时。</p>
      </div>
    </div>
  </section>

  <!-- ═══ Section 3: Three Core Capabilities ═══ -->
  <section class="section section-alt">
    <div class="container">
      <div class="section-label">核心能力</div>
      <h2 class="section-title">三层运行时，一套完整体系</h2>
      <p class="section-desc" style="margin-bottom: var(--space-2xl)">
        AEROS 不是一个工具库——而是管理具身智能体整个生命周期的运行时平台。
      </p>

      <div class="capability-grid">
        <a href="/zh/product" class="capability-card">
          <div class="capability-tag">Apache 2.0</div>
          <h3>Agent 运行时</h3>
          <ul>
            <li>生命周期管理</li>
            <li>状态持久化</li>
            <li>任务调度</li>
            <li>执行闭环</li>
            <li>故障恢复</li>
            <li>全链路追踪</li>
          </ul>
          <span class="capability-link">了解更多 &rarr;</span>
        </a>

        <a href="/zh/product#ecm" class="capability-card">
          <div class="capability-tag">Apache 2.0</div>
          <h3>ECM 包管理系统</h3>
          <ul>
            <li>标准 ECM 格式</li>
            <li>安装 · 升级 · 回滚</li>
            <li>语义化版本管理</li>
            <li>六维能力契约</li>
            <li>兼容性校验</li>
            <li>影子模式验证</li>
          </ul>
          <span class="capability-link">了解更多 &rarr;</span>
        </a>

        <a href="/zh/product#governance" class="capability-card capability-card--commercial">
          <div class="capability-tag tag-commercial">企业版</div>
          <h3>治理控制台</h3>
          <ul>
            <li>策略 DSL</li>
            <li>实时监控引擎</li>
            <li>分级恢复机制</li>
            <li>完整审计链路</li>
            <li>策略冲突检测</li>
            <li>一键版本回滚</li>
          </ul>
          <span class="capability-link">了解更多 &rarr;</span>
        </a>
      </div>
    </div>
  </section>

  <!-- ═══ Section 4: Differentiation ═══ -->
  <section class="section">
    <div class="container">
      <div class="section-label">定位</div>
      <h2 class="section-title">不是又一个机器人 OS</h2>
      <p class="section-desc" style="margin-bottom: var(--space-2xl)">
        ROS 2 管通信，AEROS 管智能体怎么跑、怎么管。<br>
        就像 Kubernetes 和 Linux——不同的层，解决不同的问题。
      </p>

      <div class="comparison-table">
        <div class="comparison-header">
          <span>传统机器人 OS</span>
          <span>AEROS</span>
        </div>
        <div class="comparison-row">
          <span>管设备通信</span>
          <span>管智能体执行</span>
        </div>
        <div class="comparison-row">
          <span>节点 + 消息</span>
          <span>Agent + ECM + 策略</span>
        </div>
        <div class="comparison-row">
          <span>部署一次就不管了</span>
          <span>持续运行 · 持续进化</span>
        </div>
        <div class="comparison-row">
          <span>出事了查不到</span>
          <span>全链路可追溯</span>
        </div>
        <div class="comparison-row">
          <span>能力写死不能改</span>
          <span>能力可插拔、可治理</span>
        </div>
      </div>

      <p class="comparison-footnote">
        ROS 2 解决通信问题。AEROS 解决智能体的<em>运行</em>和<em>治理</em>问题。<br>
        Kubernetes 不是 Linux 的插件，AEROS 也不是 ROS 2 的插件。
      </p>
    </div>
  </section>

  <!-- ═══ Section 5: Architecture ═══ -->
  <section class="section section-alt">
    <div class="container">
      <div class="section-label">架构</div>
      <h2 class="section-title">五层运行时架构</h2>

      <div class="arch-layout">
        <div class="arch-stack">
          <div class="arch-layer">
            <span class="arch-name">第 5 层：Agent</span>
            <span class="arch-desc">决策 · 推理</span>
          </div>
          <div class="arch-arrow">&darr;</div>
          <div class="arch-layer arch-governance">
            <span class="arch-name">第 4 层：治理</span>
            <span class="arch-desc">策略 · 审计</span>
          </div>
          <div class="arch-arrow">&darr;</div>
          <div class="arch-layer">
            <span class="arch-name">第 3 层：ECM 包</span>
            <span class="arch-desc">能力 · 契约</span>
          </div>
          <div class="arch-arrow">&darr;</div>
          <div class="arch-layer">
            <span class="arch-name">第 2 层：运行时</span>
            <span class="arch-desc">调度 · 执行</span>
          </div>
          <div class="arch-arrow">&darr;</div>
          <div class="arch-layer">
            <span class="arch-name">第 1 层：具身层</span>
            <span class="arch-desc">硬件 · 传感器</span>
          </div>
        </div>

        <div class="arch-text">
          <h3>治理，<br><em>从第一行代码开始</em></h3>
          <p>
            传统机器人中间件把治理当补丁——部署完了再加监控。
            AEROS 不一样。治理是运行时的一等原语，每次能力调用到达硬件前，都得先过治理引擎。
          </p>
          <p>
            结果：安全是架构保证的，不是靠祈祷。
            能力升级是受控的，不是听天由命。
            审计链路是完整的，不是抽样的。
          </p>
          <a href="/zh/technology" class="btn btn-secondary">
            深入架构设计 <span class="arrow">&rarr;</span>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ Section 6: Trust Signals ═══ -->
  <section class="section">
    <div class="container">
      <div class="section-label">学术根基</div>
      <h2 class="section-title">不是拍脑袋做的</h2>

      <div class="trust-grid">
        <div class="trust-card">
          <div class="trust-number">7</div>
          <div class="trust-label">篇论文</div>
          <div class="trust-desc">系统性研究，覆盖运行时、治理、进化、契约、联邦化、基准测试</div>
        </div>
        <div class="trust-card">
          <div class="trust-number">4</div>
          <div class="trust-label">篇 arXiv 预印本</div>
          <div class="trust-desc">全部公开，接受同行评审和学术检验</div>
        </div>
        <div class="trust-card">
          <div class="trust-number">1</div>
          <div class="trust-label">个开源 MVP</div>
          <div class="trust-desc">运行时核心 Apache 2.0 开源，拿来就能用</div>
        </div>
      </div>

      <div style="text-align: center; margin-top: var(--space-xl)">
        <a href="https://s20sc.github.io/aeros-project" target="_blank" rel="noopener" class="btn btn-secondary">
          查看研究计划 <span class="arrow">&rarr;</span>
        </a>
      </div>
    </div>
  </section>

  <!-- ═══ Section 7: CTA ═══ -->
  <section class="cta-section grid-bg">
    <div class="container" style="text-align: center">
      <h2 class="cta-title">让你的机器人<br>真正<em>可治理</em></h2>
      <p class="cta-desc">看看 AEROS 怎样让你的具身智能体跑得更稳、管得更好、进化得更快。</p>
      <div class="cta-buttons">
        <a href="/zh/contact" class="btn btn-primary">预约 Demo</a>
        <a href="/zh/open-source" class="btn btn-secondary">查看文档</a>
        <a href="https://github.com/s20sc/aeros-runtime" target="_blank" rel="noopener" class="btn btn-ghost">
          加入社区 <span class="arrow">&rarr;</span>
        </a>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  /* ═══ Hero ═══ */
  .hero {
    padding: 140px 0 120px;
    text-align: center;
    overflow: hidden;
    position: relative;
  }
  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 80% 50% at 50% -10%, rgba(0,102,255,0.12) 0%, transparent 60%);
    pointer-events: none;
  }
  .hero-content { position: relative; z-index: 2; }

  .hero-h1 {
    font-family: var(--font-display);
    font-size: clamp(3rem, 7vw, 5.5rem);
    font-weight: 400;
    line-height: 1.05;
    letter-spacing: -0.02em;
    color: var(--text-primary);
    margin-bottom: var(--space-lg);
  }
  .hero-h1 em {
    font-style: normal;
    color: var(--primary);
  }

  .hero-sub {
    font-size: 1.125rem;
    color: var(--text-secondary);
    max-width: 520px;
    margin: 0 auto var(--space-2xl);
    line-height: 1.7;
  }

  .hero-ctas {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    margin-bottom: var(--space-xl);
    flex-wrap: wrap;
  }

  .hero-badge {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--text-muted);
    letter-spacing: 0.03em;
  }

  .arrow { margin-left: 4px; }

  /* ═══ Problem Statement ═══ */
  .problem-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
    margin-bottom: var(--space-2xl);
  }

  .problem-card {
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
  }

  .problem-icon {
    font-size: 1.5rem;
    color: var(--primary);
    margin-bottom: var(--space-md);
  }

  .problem-card h3 {
    font-family: var(--font-display);
    font-size: 1.25rem;
    margin-bottom: var(--space-sm);
    color: var(--text-primary);
  }

  .problem-card p {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .problem-conclusion {
    text-align: center;
    padding: var(--space-xl);
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
  }

  .problem-conclusion p {
    font-size: 1.125rem;
    color: var(--text-secondary);
    max-width: 640px;
    margin: 0 auto;
  }
  .problem-conclusion strong { color: var(--text-primary); }

  /* ═══ Section Alt Background ═══ */
  .section-alt {
    background: var(--bg-surface);
    border-top: 1px solid var(--border-subtle);
    border-bottom: 1px solid var(--border-subtle);
  }

  /* ═══ Capabilities ═══ */
  .capability-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
  }

  .capability-card {
    display: flex;
    flex-direction: column;
    background: var(--bg-base);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    transition: border-color var(--duration-short), transform var(--duration-short);
  }
  .capability-card:hover {
    border-color: rgba(0, 102, 255, 0.3);
    transform: translateY(-2px);
  }
  .capability-card--commercial {
    border-color: rgba(0, 194, 168, 0.2);
  }
  .capability-card--commercial:hover {
    border-color: rgba(0, 194, 168, 0.4);
  }

  .capability-tag {
    font-family: var(--font-mono);
    font-size: 11px;
    padding: 3px 10px;
    border-radius: var(--radius-full);
    background: rgba(0, 102, 255, 0.1);
    color: var(--primary);
    width: fit-content;
    margin-bottom: var(--space-md);
  }
  .tag-commercial {
    background: rgba(0, 194, 168, 0.1);
    color: var(--secondary);
  }

  .capability-card h3 {
    font-family: var(--font-display);
    font-size: 1.5rem;
    margin-bottom: var(--space-md);
    color: var(--text-primary);
  }

  .capability-card ul {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    margin-bottom: var(--space-lg);
  }

  .capability-card li {
    font-size: 14px;
    color: var(--text-secondary);
    padding-left: var(--space-md);
    position: relative;
  }
  .capability-card li::before {
    content: '\00B7';
    position: absolute;
    left: 0;
    color: var(--text-muted);
  }

  .capability-link {
    font-size: 14px;
    font-weight: 500;
    color: var(--primary);
  }

  /* ═══ Comparison Table ═══ */
  .comparison-table {
    max-width: 800px;
    margin: 0 auto var(--space-xl);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    overflow: hidden;
  }

  .comparison-header {
    display: grid;
    grid-template-columns: 1fr 1fr;
    background: var(--bg-surface);
    border-bottom: 1px solid var(--border);
  }

  .comparison-header span {
    padding: var(--space-md) var(--space-lg);
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--text-muted);
  }
  .comparison-header span:last-child {
    color: var(--primary);
    border-left: 1px solid var(--border);
  }

  .comparison-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    border-bottom: 1px solid var(--border-subtle);
  }
  .comparison-row:last-child { border-bottom: none; }

  .comparison-row span {
    padding: var(--space-md) var(--space-lg);
    font-size: 14px;
    color: var(--text-secondary);
  }
  .comparison-row span:last-child {
    color: var(--text-primary);
    font-weight: 500;
    border-left: 1px solid var(--border-subtle);
    background: rgba(0, 102, 255, 0.02);
  }

  .comparison-footnote {
    text-align: center;
    font-size: 14px;
    color: var(--text-muted);
    max-width: 640px;
    margin: 0 auto;
    line-height: 1.6;
  }
  .comparison-footnote em {
    font-family: var(--font-display);
    font-style: italic;
    color: var(--text-secondary);
  }

  /* ═══ Architecture ═══ */
  .arch-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3xl);
    align-items: center;
  }

  .arch-stack {
    background: var(--bg-base);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
  }

  .arch-layer {
    background: var(--bg-elevated);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-sm);
    padding: var(--space-sm) var(--space-md);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .arch-governance {
    border-color: rgba(0, 194, 168, 0.3);
    background: rgba(0, 194, 168, 0.05);
  }

  .arch-name {
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 500;
    color: var(--text-primary);
  }

  .arch-desc {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--text-muted);
  }

  .arch-arrow {
    text-align: center;
    color: var(--text-muted);
    font-size: 10px;
    padding: var(--space-xs) 0;
    font-family: var(--font-mono);
  }

  .arch-text h3 {
    font-family: var(--font-display);
    font-size: 2rem;
    color: var(--text-primary);
    margin-bottom: var(--space-md);
    line-height: 1.2;
  }
  .arch-text em {
    font-style: normal;
    color: var(--primary);
  }
  .arch-text p {
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: var(--space-lg);
    font-size: 15px;
  }

  /* ═══ Trust Signals ═══ */
  .trust-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
  }

  .trust-card {
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    text-align: center;
  }

  .trust-number {
    font-family: var(--font-mono);
    font-size: 3.5rem;
    font-weight: 600;
    color: var(--primary);
    line-height: 1;
    margin-bottom: var(--space-xs);
  }

  .trust-label {
    font-family: var(--font-display);
    font-size: 1.25rem;
    color: var(--text-primary);
    margin-bottom: var(--space-sm);
  }

  .trust-desc {
    font-size: 14px;
    color: var(--text-muted);
    line-height: 1.5;
  }

  /* ═══ CTA Section ═══ */
  .cta-section {
    padding: var(--space-4xl) 0;
    overflow: hidden;
    position: relative;
  }
  .cta-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 60% 40% at 50% 100%, rgba(0,102,255,0.06) 0%, transparent 70%);
    pointer-events: none;
  }

  .cta-title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 5vw, 3.5rem);
    color: var(--text-primary);
    margin-bottom: var(--space-md);
    line-height: 1.15;
    position: relative;
  }
  .cta-title em {
    font-style: normal;
    color: var(--primary);
  }

  .cta-desc {
    color: var(--text-secondary);
    font-size: 1.125rem;
    margin-bottom: var(--space-xl);
    position: relative;
  }

  .cta-buttons {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    flex-wrap: wrap;
    position: relative;
  }

  /* ═══ Responsive ═══ */
  @media (max-width: 768px) {
    .hero { padding: 100px 0 80px; }
    .problem-grid,
    .capability-grid,
    .trust-grid { grid-template-columns: 1fr; }
    .arch-layout { grid-template-columns: 1fr; }
    .comparison-header span,
    .comparison-row span { padding: var(--space-sm) var(--space-md); font-size: 13px; }
  }
</style>
